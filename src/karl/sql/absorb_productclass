update
	product
set
	product.productclass_id = $2
where
	product.productclass_id = $1;

update
	tag_productclass
set
	tag_productclass.productclass_id = $2
where
	tag_productclass.productclass_id = $1;

delete productclass where productclass.id = $1;
