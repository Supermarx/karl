insert into
	productdetails (
		product_id,
		name,
		volume,
		volume_measure,
		orig_price,
		price,
		discount_amount,
		valid_on,
		valid_until,
		retrieved_on
	)
values (
	$1, $2, $3, $4, $5, $6, $7, $8, NULL, $9
)
returning id
