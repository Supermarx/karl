select
	product.id,
	product.identifier,
	product.supermarket_id,
	product.imagecitation_id,
	product.productclass_id,
	product.name,
	product.volume,
	product.volume_measure
from
	product
where
	product.identifier = $1 and
	product.supermarket_id = $2
